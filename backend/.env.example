# Backend local development environment

PORT=3001
DATABASE_URL=postgres://postgres:postgres@localhost:5432/gigit

# Development-only JWT settings (replace in real deployments)
JWT_ISSUER=https://example.invalid/
JWT_AUDIENCE=gigit
JWT_JWKS_URL=https://example.invalid/.well-known/jwks.json

